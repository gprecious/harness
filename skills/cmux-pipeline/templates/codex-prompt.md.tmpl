# Phase {{PHASE_ID}}: {{PHASE_TITLE}}

## Task
{{PHASE_GOAL}}

## Ownership
- Files allowed to touch:
{{ALLOW_FILES}}
- Files NOT to touch:
{{DENY_FILES}}

## Context
- Run: {{RUN_ID}}
- Repo root: {{REPO_ROOT}}
- Feature branch: {{FEATURE_BRANCH}}
- Prior phase outputs:
{{PRIOR_OUTPUTS}}
- Repo conventions:
  - Test command: `{{TEST_COMMAND}}`
  - Type check: `{{TYPECHECK_COMMAND}}`
  - Lint: `{{LINT_COMMAND}}`

## Interface Contract
{{INTERFACE_CONTRACT}}

## Validation
- [ ] Tests at {{TEST_PATHS}} all pass
- [ ] `{{TYPECHECK_COMMAND}}` passes
- [ ] No new lint errors
- [ ] git commit with message: `feat({{TOPIC}}): phase-{{PHASE_ID}} {{PHASE_TITLE}}`
{{CONTRACT_REFERENCE}}
## Return Format

When you complete ALL deliverables above (or determine you cannot), write
EXACTLY this line on its own line at the very end and stop:

==DONE=={{RUN_ID}}==phase-{{PHASE_ID}}==<status>==

Where <status> is one of:
- SUCCESS — 모든 validation 통과
- NEEDS_HELP — spec 모호, 추가 정보 필요
- FAILED — 명백한 실패 (구현 불가능, 의존성 부재 등)
